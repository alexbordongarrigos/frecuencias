import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.omnifrequency.app',
  appName: 'OmniFrequency',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
